[package]
name = "crypto"
version = "0.1.0"
authors = ["aion <admin@aion.network>"]
edition = "2018"
build = "build.rs"

[build-dependencies]
gcc = "^0.3"

[dependencies]
rustc-serialize = "^0.3"
rand = "0.5.5"
libc = "*"

[features]
benches = []